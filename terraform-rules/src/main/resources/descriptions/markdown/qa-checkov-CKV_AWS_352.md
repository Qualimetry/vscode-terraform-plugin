# Ensure NACL ingress does not allow all Ports

`qa-checkov-CKV_AWS_352` &middot; Security &middot; Vulnerability &middot; severity MAJOR

## Summary

Ensure NACL ingress does not allow all Ports.

## Noncompliant code example

```hcl
resource "aws_s3_bucket_acl" "a" { bucket = aws_s3_bucket.a.id; acl = "public-read" }
```

## Compliant solution

```hcl
resource "aws_s3_bucket_acl" "a" { bucket = aws_s3_bucket.a.id; acl = "private" }
```

## See also

- [https://www.checkov.io/5.Policy%20Index/terraform.html](https://www.checkov.io/5.Policy%20Index/terraform.html)
